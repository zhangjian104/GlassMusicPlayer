---
description: 提问与任务执行前置检查清单
alwaysApply: true
---

# AI 提问与任务执行前置规范

在处理用户的每个新提问或新任务时，AI 必须遵循以下前置规范：

## 1. 分析与确认

- 在开始编写代码前，必须先理清当前修改的**范围**（是仅仅修改某一个方法，还是涉及桌面端和移动端的多个组件）。
- 如果修改涉及到多个组件或文件（如同时影响 `src/pages/index.vue` 和 `src/pages/mobile/index.vue`），必须**一次性列出完整的修改规划**。
- 不确定时必须先向用户提问确认，不要自行盲目猜测或直接修改代码。

## 2. 交互风格要求

- 所有的思考过程（如果可见）、问题回复、计划描述以及代码注释，**必须使用中文**。
- 只有在真正需要的时候（用户明确要求写出文档）才生成说明性的 `.md` 文档。
- 当完成任务或向用户展示修改时，直接描述你做了什么，不要出现诸如“很高兴为您服务”之类的多余套话，保持精简。

## 3. 避免破坏性修改

- 必须尊重项目现有的代码风格和架构（例如现有的 Tailwind 响应式布局、Pinia 状态管理方式）。
- 绝不要在无业务必要的情况下删除原有的、还能用的其他功能代码（例如只要求改图片链接时，绝不能擅自重构旁边的标题逻辑）。
- 修改前仔细审视代码周围的注释和逻辑，特别是对于已经从“音乐”强行修改为“有声小说”相关的逻辑，要**保持兼容，不产生回退**。

## 4. 当用户提问项目内的实现细节时，可以使用Mermaid语法生成流程图或类图来辅助描述。

## 5. 注意要基于现有的代码进行解读，如果我取消了你的某一笔代码更改，那么这个更改相关的内容应该从上下文中删除，以免污染上下文。
